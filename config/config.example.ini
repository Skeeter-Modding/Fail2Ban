[discord]
# Discord Bot Token (required for bot functionality)
# Get this from https://discord.com/developers/applications
bot_token = YOUR_BOT_TOKEN_HERE

# Discord Webhook URL (for notifications only, alternative to bot)
# Create in Discord: Server Settings > Integrations > Webhooks
webhook_url = YOUR_WEBHOOK_URL_HERE

# Channel ID where the bot will send notifications and accept commands
channel_id = YOUR_CHANNEL_ID_HERE

# Role ID that can execute admin commands (optional, leave empty for any user)
admin_role_id =

# Guild/Server ID (required for slash commands)
guild_id = YOUR_GUILD_ID_HERE

[fail2ban]
# Path to fail2ban-client
fail2ban_client = /usr/bin/fail2ban-client

# Jails to monitor (comma-separated)
# Leave empty to monitor all jails
monitored_jails = sshd,arma-reforger

# Log file path for the bot
log_file = /var/log/fail2ban-discord.log

[notifications]
# Enable/disable different notification types
notify_on_ban = true
notify_on_unban = true
notify_on_start = true
notify_on_stop = true

# Include GeoIP information in ban notifications (requires geoip2 package)
include_geoip = true

# Embed color (hex without #)
ban_color = ff0000
unban_color = 00ff00
info_color = 0099ff

[abuseipdb]
# AbuseIPDB API Key (get free key at https://www.abuseipdb.com/account/api)
# Leave empty to disable AbuseIPDB integration
api_key =

# Automatically report banned IPs to AbuseIPDB
auto_report = false

# Only auto-report IPs with abuse score below this threshold
# (prevents re-reporting already known bad IPs)
report_threshold = 50

[attack_detection]
# Enable attack detection alerts
enabled = true

# Number of bans within the alert window to trigger an attack alert
alert_threshold = 10

# Time window in seconds to check for attack conditions
alert_window = 60

# Cooldown in seconds between attack alerts (prevent spam)
alert_cooldown = 300

[reports]
# Send daily statistics report (at 8 AM server time)
daily_report = false

# Send weekly statistics report (Monday at 8 AM)
weekly_report = false

[arma_reforger]
# Path to Arma Reforger server logs
# For Docker: typically mounted volume path
log_path = /var/log/arma-reforger/

# Container name pattern (for Docker log access)
container_pattern = arma-reforger-*

# Ban thresholds
max_retry = 5
find_time = 600
ban_time = 3600
